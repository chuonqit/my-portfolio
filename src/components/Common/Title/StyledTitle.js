import styled from 'styled-components';

export const Title = styled.h1`
  display: block;
  color: var(--light);
  font-size: 3rem;
  font-weight: 700;
  word-break: break-word;
  margin-right: -20vw;
  padding-right: 2rem;
`;
